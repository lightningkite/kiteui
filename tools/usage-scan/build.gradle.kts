// Root of the standalone usage-scan tool build.
// Intentionally NOT part of the kiteui settings.gradle.kts so it never affects the library build.
plugins {
    kotlin("jvm") version "2.3.21" apply false
}
